import { FilterPetCategoryPipe } from './filter-pet-category.pipe';

describe('FilterPetCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPetCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
