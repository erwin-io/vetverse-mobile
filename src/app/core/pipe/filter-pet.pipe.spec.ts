import { FilterPetPipe } from './filter-pet.pipe';

describe('FilterPetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPetPipe();
    expect(pipe).toBeTruthy();
  });
});
