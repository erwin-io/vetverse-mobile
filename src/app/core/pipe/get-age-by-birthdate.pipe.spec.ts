import { GetAgeByBirthdatePipe } from './get-age-by-birthdate.pipe';

describe('GetAgeByBirthdatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetAgeByBirthdatePipe();
    expect(pipe).toBeTruthy();
  });
});
